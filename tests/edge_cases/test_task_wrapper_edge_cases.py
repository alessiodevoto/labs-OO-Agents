"""Edge cases for TaskWrapper.

Focus on:
- `task.abort()` called from nested method
- `task.replan()` called from REPL during generation
- `task.current()` accessed from nested method
"""

# TODO: Add task wrapper edge case tests
