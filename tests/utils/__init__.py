"""Utility tests - Pure Python modules.

Tests focus on:
- message utility (message.send())
- context utility (context.set/get)
- logger utility (structured logging)
- task utility (task.current/abort/replan)
"""
